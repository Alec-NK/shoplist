export type AddProductProps = {
  product_name: string;
  price: string;
  quantity: string;
  category: string;
};

export type ProductType = {
  title: string;
  price: number;
  quantity: number;
  category: string;
};
